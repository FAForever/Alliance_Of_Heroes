-- DefautTemplates
-- Alliance of Heroes 2018

DefautTemplates = {
	SetTrainingWeight = function(id, baseclass, prestigeclass)
	
	end,
	SetArmorModifiers = function(id, baseclass, prestigeclass)
	
	end,
	SetWeaponModifiers = function(id, baseclass, prestigeclass)
	
	end,
}